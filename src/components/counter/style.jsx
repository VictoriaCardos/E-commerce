import styled from 'styled-components'

export const Content = styled.div`
  span {
    display: inline-flex;
    flex-direction: row;
  }

  button {
    border: none;
    background: transparent;
  }

  button img {
    width: 1.5rem;
  }

  .trash {
    width: 2.5rem;
  }

  span button {
    width: 2.5rem;
  }

  h4 {
    margin-top: 0.6rem;
  }
`
