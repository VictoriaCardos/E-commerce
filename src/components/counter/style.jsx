import styled from 'styled-components'

export const Content = styled.div`
  span {
    display: inline-flex;
    flex-direction: row;
  }

  button {
    border: none;
    background: transparent;
  }

  button img {
    width: 2rem;
  }

  span button {
    width: 4rem;
  }

  h4 {
    margin-top: 1.5rem;
  }
`
